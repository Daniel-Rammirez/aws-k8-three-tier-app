apiVersion: v1
kind: Secret
metadata:
  name: frontend-secret
  namespace: flight-app
type: Opaque
data: {}
# No runtime secrets needed for the frontend.
# NEXT_PUBLIC_* vars are baked into the Docker image at build time.
