apiVersion: v1
kind: Service
metadata:
  name: flight-db
  namespace: flight-app
spec:
  selector:
    app: flight-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
