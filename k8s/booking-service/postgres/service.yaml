apiVersion: v1
kind: Service
metadata:
  name: booking-db
  namespace: flight-app
spec:
  selector:
    app: booking-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
